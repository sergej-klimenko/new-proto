version: 0.2
phases:
  install:
    commands:
      - go mod download
  build:
    commands:
      - cd tests
      - go test -coverpkg ./... ./... | { grep -v 'no test files'; true; }
  post_build:
    commands:
      - echo Deploying...
